import Foundation

/// Master switch for demo recording / one-shot playback tools on the whiteboard.
/// Set to `false` to strip all demo tooling from the UI and disable related behavior.
enum DemoDebug {
    static let isEnabled = true
}
